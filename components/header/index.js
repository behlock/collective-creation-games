import s from './header.module.scss'

export const Header = () => {
  // const isMobile = useMediaQuery('(max-width: 800px)')
  // const visible = usePageAppear()

  return <header className={s.container}></header>
}
